# Publicação estática do aplicativo original de arquivo único (site/index.html).
# Sem build: o HTML é servido exatamente como está, com todo CSS/JS/bibliotecas inline.
[build]
  publish = "site"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
